import { PercentRoundPipe } from './percent-round.pipe';

describe('PercentRoundPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentRoundPipe();
    expect(pipe).toBeTruthy();
  });
});
